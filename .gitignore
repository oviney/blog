# Ruby vendor bundle
vendor/

# Jekyll
_site
.sass-cache
.jekyll-metadata
.backup
.jekyll-cache
Gemfile.lock
.Gemfile
.githubauth%

# Testing
node_modules/
backstop_data/bitmaps_test/
backstop_data/html_report/
.playwright-mcp/
.lighthouseci/
chrome/

# Playwright
test-results/
playwright-report/
*.webm
*.png
!assets/charts/*.png
!assets/images/*.png
# Committed visual-regression baselines (generated on ubuntu-latest; see
# tests/playwright-agents/visual-snapshot.spec.ts).
!tests/playwright-agents/**/*-snapshots/*.png
**/screenshots/
**/videos/
playwright-healing-report.md
quality-report.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# Agent outputs
content_queue.json
board_decision.json
board_report.md
test_outputs/
model_comparison_results.json
content-review-results.json

# Secrets
.env
*.key
*_API_KEY*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude settings
.claude/settings.local.json

# Vendor lifecycle skills (upstream: addyosmani/agent-skills @ refs/tags/0.6.0)
# Synced locally as tooling; not part of the site or its build.
.agents/

# Healing monitoring (runtime data)
healing-metrics/
healing-reports/
healing-success-report.md
.bundle/

# Ruby vendor directory
vendor/

# Parallel-agent worktrees (local-only — isolated agent sessions on issue
# branches; never synced and excluded from the Jekyll build)
worktrees/
.worktrees/

# Claude Code session-scheduler lock (local-only; per-session state)
.claude/scheduled_tasks.lock

# Agent-PR rubric scorecards (scripts/eval-agent-pr.sh output; ephemeral,
# regenerated per run in CI and locally — never committed)
.agent-evals/
